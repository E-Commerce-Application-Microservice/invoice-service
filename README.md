# Invoice Service
Invoice generation and management.

## Endpoints
- `POST /invoices/generate` — Generate invoice
- `GET /invoices/:orderId` — Get invoice by order
- `GET /invoices` — List all invoices (admin)
- `GET /health` — Health check
